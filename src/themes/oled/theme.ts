import type { ThemeDefinition } from "../theme.types";

export const OLED_THEME: ThemeDefinition = {
  id: "oled",
  label: "OLED",
  description: "Pure black, quiet surfaces and artwork-led colour.",
};
